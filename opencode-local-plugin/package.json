{
    "name": "opencode-local-plugin",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "dev": "tsc -p tsconfig.json -w",
        "postbuild": "node ./scripts/sync-to-opencode.mjs"
    },
    "dependencies": {
        "@opencode-ai/plugin": "latest",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@types/node": "^25.2.3",
        "esbuild": "^0.27.3",
        "typescript": "^5.6.0"
    }
}
